#include <stdio.h>
#include <stdlib.h>
/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
int lo;
for (lo = '0' ; lo <= '9' ; lo++)
{
putchar(lo);
putchar(',');
putchar(' ');
 if ( lo > '9')
  
}
putchar('\n');
return (0);
}
