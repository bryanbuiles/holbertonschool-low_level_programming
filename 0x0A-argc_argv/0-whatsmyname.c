#include <stdio.h>

/**
 * main - main function
 * @argc: Number of arguments
 * @argv: Array of arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{

	if (argc >= 1)
{
	printf("%s\n", argv[0]);
}
return (0);
}
