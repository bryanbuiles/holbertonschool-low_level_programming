#include "holberton.h"
#include <math.h>
/**
  * _abs - less  more or zero
  * @n: parameter
  * Return: 1 is lowercase or 0 if not
  */
int _abs(int n)
{
	if (n >= 0)
	return (n);
	else
	return (n = n * -1);
}
